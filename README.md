# Real time forume 

* 
    sqlite && go && js && html && css
    One Page HTML => Single Page Application 

* 
    Registration and login 
        *register ; Nickname Age Gender First Name Last Name E-mail Password
        *login : be able to connect with nickName or email  
        *The user must be able to log out from any page on the forum.

    Post and comment 
        *Posts will have categories as in the first forum
        *Create comments on the posts
        *See posts in a feed display
        *See comments only if they click on a post

    Private Messages
        (apres)


* 
    webSocket 
    bcrypt 
    UUID
    SQLite3





Menaces ;
	query := "SELECT * FROM users  WHERE " + column + " = ?" => On peut Injecter des injections SQL 